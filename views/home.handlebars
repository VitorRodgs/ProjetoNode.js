<h1>Lista de Posts</h1>

{{#each posts}}

    <h1>{{titulo}}</h1>
    <p>{{conteudo}}</p>
    <small>{{dataValues.createdAt}}</small>
    <a href="/deletar/{{dataValues.id}}"><button>Deletar Postagem</button></a>
    <hr>

{{/each}}